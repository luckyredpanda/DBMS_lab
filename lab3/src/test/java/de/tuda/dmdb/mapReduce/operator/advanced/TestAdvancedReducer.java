package de.tuda.dmdb.mapReduce.operator.advanced;

import de.tuda.dmdb.TestCase;
import org.junit.jupiter.api.Test;

import org.junit.jupiter.api.Disabled;
@Disabled("Disabled until you implement advanced tests") 
public class TestAdvancedReducer extends TestCase {

  /**
   * Tests the Reducer with the word count example. I.e. passed reducer function counts together
   * values (occurrences) within each group (word)
   */
  @Test
  public void testReducerWordCount() {}
}
